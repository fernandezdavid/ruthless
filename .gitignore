node_modules/
.DS_Store
*.log
.env
.env.local

# Editor
.vscode/
.idea/

# Agent-specific local settings (user-scoped, not part of the distributed skill)
.claude/settings.local.json
.cursor/settings.local.json
.agents/settings.local.json

# NOTE: Generated provider directories (.claude, .cursor, .gemini, .codex, .agents)
# are NOT ignored. They are committed so users can install Ruthless by copying
# them directly without needing to run the build system. Only user-specific
# local settings files within them are ignored (see above).

# Internal working docs — used to plan follow-up work but not part of the
# distributed repo. Keep in the working tree, never commit.
FOLLOW_UP.md
